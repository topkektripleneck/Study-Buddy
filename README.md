# BuddyUp

Fresh Tauri 2 + Vite + TypeScript starter.

## Setup

```bash
npm install
npm run tauri dev
```

## Scripts

- `npm run dev` — Vite frontend only
- `npm run tauri dev` — full desktop app
- `npm run tauri build` — production build
